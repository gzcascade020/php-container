#!/bin/sh

if ! whoami 2>&1 >/dev/null; then
  if [ -w /etc/passwd ]; then
    echo "${USER_NAME:-default}:x:$(id -u):0:Default Application User:${HOME}:/sbin/nologin" >> /etc/passwd
    echo "${USER_NAME:-default}:!:18211:0:99999:7:::" >> /etc/shadow
  fi
fi

exec "$@"